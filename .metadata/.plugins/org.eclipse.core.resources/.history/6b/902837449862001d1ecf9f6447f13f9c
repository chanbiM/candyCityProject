package dao;

import javax.servlet.http.HttpSession;

import vo.MemberVO;
import vo.PostVO;

public class PostDAO {
	public int insertPost(PostVO vo) {
		ttpSession session = request.getSession();
		
		MemberVO MemberVo = (MemberVO) Http session.getAttribute("loginOK");
		return 0;
	}
}
